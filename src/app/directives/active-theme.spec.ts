import { ActiveTheme } from './active-theme';

describe('ActiveTheme', () => {
  it('should create an instance', () => {
    const directive = new ActiveTheme();
    expect(directive).toBeTruthy();
  });
});
